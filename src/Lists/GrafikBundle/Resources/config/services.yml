parameters:
    lists_grafik.service.class: Lists\GrafikBundle\Services\GrafikService

services:
    lists_grafik.service:
        class: %lists_grafik.service.class%
        arguments: [ @service_container ]
