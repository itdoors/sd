{% extends "::base.html.twig" %}

{% block body %}

    Size of organizations = {{ organizations|length }}
    {% if organizations|length > 0 %}
        {% for organization in organizations %}
            {{ organization.name|e }} <br />
        {% endfor %}
    {% endif %}

{% endblock body %}