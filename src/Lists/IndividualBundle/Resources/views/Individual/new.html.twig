{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Individual creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('individual') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
