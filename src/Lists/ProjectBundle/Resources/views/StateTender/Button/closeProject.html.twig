<a class="btn green" id="close-add-btn-{{object.id}}" href="#" data-toggle="modal">
    {% trans from 'ListsProjectBundle'%}Close project{% endtrans %}
</a>
{{ ajax_form_render_btn({
   'selector' : '#close-add-btn-'~ object.id,
   'formType' : 'closeProjectForm',
   'successFunctions' : {
        'project_edit' : 'ITDoorsAjax.updateList',
        'list_participants' : 'ITDoorsAjax.updateList',
        'list_documents' : 'ITDoorsAjax.updateList'
   },
   'serviceSave' : {
       'alias' : 'lists_handling.service',
       'method' : 'saveCloseProjectForm'
   },
   'defaults' : {
       'projectId' : object.id
   },
   'target' : '#form_modal_closeProjectForm'~object.id,
   'html' : 'ListsProjectBundle:Form:closeProjectForm.html.twig'
}) }}
<div id="form_modal_closeProjectForm{{object.id}}" class="modal" role="basic" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true"></button>
                <h4 class="modal-title">{% trans from 'ListsHandlingBundle' %}Close project{% endtrans %}</h4>
            </div>
            <div class="modal-body">
                <div data-text="{% trans %}Loading data. Please wait{% endtrans %}"></div>
            </div>
        </div>
    </div>
</div>