{% trans_default_domain 'ITDoorsControllingBundle' %}
<div class="row">
    <div class="col-md-12">
        <div class="tabbable tabbable-custom boxless">
             {{ ajax_tab_render( tabs, tab, namespace) }}
            <div class="tab-content ajax-tab-holder-1" id="datatable_ajax_1" data-url="{{ url('it_doors_controlling_invoice_expected_data_show', { period: tab }) }}">
                 {{ render(controller('ITDoorsControllingBundle:Invoice:expecteddatashow')) }}
            </div>
        </div>
    </div>
</div>